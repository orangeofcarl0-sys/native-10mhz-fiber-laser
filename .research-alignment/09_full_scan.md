# 完整双网格重算（2026-09-19）

问题：优化后的 FP64 GPU 核心是否能完整复算既定 OC × GDD × pump 地图，且保留真实的未解析与不稳定结果。

范围：两拓扑各 5 个 GDD，每组 8 OC × 8 pump；粗/细各 640 点，共 1280 条轨迹，每点上限 600 圈。使用 scan.py 的原始 CPU 精确诊断，不使用 resident 近似峰数筛选。求解器版本 33fea24；参数、代码和 40 个原始文件的 SHA256 已记录。

证据：20 组完整结束，20 组初始复场/反转及参数标签与历史输入完全一致。总用时 1939.95 s，实际计算 279760 案例圈。粗网格 104 点通过边界检查、3 点目标范围末段单峰；细网格分别为 264、8。严格短期筛选均为 0。

对照：粗网格分类 640/640 一致，细网格 638/640 一致。g04_c63 从完成变为触边；g08_c01 从触边变为完成，但新均值仅 0.04037 nJ，不能作为目标候选。两点前 20 圈能量相对差均约 6e-14，后续放大。该证据不足以证明混沌或指定分岔机制；不强行替换新结果，也不宣称全程逐点等价。

代表：细网格 g03_c11，OC→CNT、OC30%、GDD+0.2 ps²、pump20 mW，末100圈 0.139–0.249 nJ，CV18.97%，末圈FWHM52.625 ps。仍为非稳态单峰，不能认证稳定锁模。1.024 ns 局部窗口不能排除整个100 ns腔周期内的远端脉冲。

报告：主地图和表格来自本次细网格；第12–20节明确保留为历史长程验证。新增第31节提供完整覆盖、差异曲线、代表状态和1280行导出。原始 MAT 仅本地保存，公开仓库保留可审计摘要。
