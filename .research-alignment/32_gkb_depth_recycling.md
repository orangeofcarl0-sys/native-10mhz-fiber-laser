# 最新末态GKB深度与空间回收冻结审计

起点为纯GKB40步的states[-1]，R=0.0009746756121000674；旧状态为states[-2]，即生成第40接受步的状态。固定Δ=.025，泵浦、OC、GDD、Psat、网格、gauge及状态度量不变。无outer更新、旧C预条件器或multiple shooting。

当前状态一次连续fresh GKB到192维，读取64/80/96/112/128/144/160/176/192；不因观察结果提前停。所有checkpoint均经真实cavity映射、三尺度Js、Cauchy、可行性及rho检查。记录每16维绝对/相对模型增量、实际下降、步长、lambda、B全奇异谱和累计构造耗时。

重建旧状态64维GKB，在当前状态重算64个Jv；不沿用旧响应。比较K64、R64、正交合并K64+R64、K128、K192。合并用QR+SVD(相对截断1e-12)获得状态正交基，响应同步变换，保持真实状态欧氏trust metric。不是过去的梯度history bank。

计时区分完整审计成本与缓存可用时的warm回收成本；后者不含历史上已发生的旧空间构造，包含当前K64、64次当前Jv、正交合并和small solve；同时明确列出cold总成本。同步墙钟，不承诺outer速度。

冻结诊断：连续两次16维相对模型增益在[0,1%)标记深度饱和；仍完整到192。回收分别与K128和K192比较：模型覆盖>90%、通过真实保护且warm成本<=对应fresh成本80%，记为该深度的回收支持信号。“几乎无增益”操作化为联合空间相对K64的模型收益<=5%。这些是本末态诊断，不自动切换生产算法；未饱和不证明完整Jacobian不存在病态。

验证：重复空间已知解/度量测试；旧状态K64预测复现上一轮末步；当前64新基线与旧状态64区分；正交、JV-UB、合并rank/条件谱、真实候选独立重放；原始输入哈希、执行源、候选及本地矩阵归档；HTML独立大图。公共结果包含重建脚本、精简复核数据；大矩阵留本地，不必把整套矩阵重复推送Git。

## 观测与决策

192维无饱和（176→192模型增益4.923%），但同半径K144–K192真实merit上升。K96真实下降3.42293e-9、rho1.16892，是本组最佳通过候选；K128下降1.03878e-9、rho.20060。联合回收下降1.37287e-9、rho.26880，模型覆盖K128为98.63095%、K192为75.994%。独立计时联合warm52.393s、cold84.511s、K9659.802s、K12884.412s、K192134.868s。回收对K128达到预设支持门，对K192未达到；不据此忽略K96更好的真实下降/成本。

单独R64通过局部rho/Js但不满足当前Cauchy下界，不能独立接受。联合rank128，物理状态正交误差2.83e-14。11个原始候选独立复核一致，三尺度预测最大相对误差2.53e-6；104项测试通过。报告13节6独立图，离线与移动宽度检查通过。未调整半径，未继续outer，未引入预条件或multiple shooting。

限定解释：扩大深度确有价值，但有限步长非线性现在参与限制，不能延续64维时的全局化判断。重用完整空间显示价值，却尚不能单凭覆盖率选为唯一主线。
